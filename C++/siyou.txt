    void sum(type a,type b){
        
    }
みたいに関数を書くことがよくあるけどここでの引数の仕組みは多分だけど
type a=呼び出し元で入れた値;
type b=呼び出し元で入れた値;
を最初に行っているような感じになってる。だから
void sum(vector<int>& v)のような渡し方が出来る。
vector<int>& v=参照元の配列;